import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 10px;
  line-height: 1;
  text-transform: uppercase;

  span {
    font-size: 10px;
    opacity: 0.8;
  }

  p {
    font-weight: 600;
  }
`;
